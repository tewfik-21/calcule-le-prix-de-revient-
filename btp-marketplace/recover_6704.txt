Created At: 2026-08-25T16:54:44Z
Completed At: 2026-08-25T16:54:47Z

				The command completed successfully.
				Output:
				import React from 'react';
import { X, Trash2, Package, RefreshCw } from 'lucide-react';
import type { Listing, UserSession } from '../../types';

interface MyAdsModalProps {
  onClose: () => void;
  user: UserSession | null;
  listings: Listing[];
  onDeleteAd: (id: string) => void;
  onRenewAd: (id: string) => void;
  lang: 'fr' | 'ar' | 'en';
}

export const MyAdsModal: React.FC<MyAdsModalProps> = ({ onClose, user, listings, onDeleteAd, onRenewAd, lang }) => {
  
  const myAds = listings.filter(l => l.sellerId === user?.id);
  const isExpired = (dateString: string, isPremium: boolean) => {
    const diff = Date.now() - new Date(dateString).getTime();
    const maxDays = isPremium ? 90 : 30;
    return diff > maxDays * 24 * 60 * 60 * 1000;
  };


  return (
    <div className="fixed inset-0 z-50 overflow-y-auto flex items-center justify-center p-4">
      <div 
        className="fixed inset-0 bg-slate-950/80 backdrop-blur-sm"
        onClick={onClose}
      />
      
      <div className="relative w-full max-w-2xl bg-slate-900 border border-white/10 rounded-3xl shadow-2xl overflow-hidden flex flex-col max-h-[85vh]">
        <div className="p-4 border-b border-white/10 flex items-center justify-between bg-slate-900/50 relative">
          <div className="flex items-center gap-3">
            <div className="h-10 w-10 rounded-xl bg-orange-500/20 text-orange-500 flex items-center justify-center">
              <Package className="h-5 w-5" />
            </div>
            <div>
              <h2 className="text-lg font-black text-white uppercase tracking-wider">
                {lang === 'ar' ? '�"�ŝ�S' : lang === 'fr' ? 'Mes Annonces' : 'My Ads'}
              </h2>
              <p className="text-[10px] text-slate-400 font-medium">
                {lang === 'ar' ? '� �"�ŝ� �"�ŝ�' : lang === 'fr' ? 'GǸrer vos annonces actives' : 'Manage your active ads'}
              </p>
            </div>
          </div>
          <button 
            onClick={onClose}
            className="p-2 hover:bg-white/5 rounded-xl transition text-slate-400 hover:text-white"
          >
            <X className="h-5 w-5" />
          </button>
        </div>

        <div className="p-6 overflow-y-auto custom-scrollbar flex-1">
          {myAds.length === 0 ? (
            <div className="flex flex-col items-center justify-center py-12 text-slate-500">
              <Package className="h-16 w-16 mb-4 opacity-20" />
              <p>{lang === 'ar' ? '�"� �S�^� �"�ŝ� �"�S�<' : lang === 'fr' ? 'Aucune annonce pour le moment' : 'No ads yet'}</p>
            </div>
          ) : (
            <div className="space-y-4">
              {myAds.map(ad => (
                <div key={ad.id} className="bg-slate-800/50 border border-white/5 p-4 rounded-2xl flex flex-col gap-3">
                  <div className="flex items-center gap-4">
                    <div className="h-16 w-16 rounded-xl overflow-hidden shrink-0">
                      <img src={ad.images?.[0] || 'https://via.placeholder.com/150'} alt={ad.title} className="h-full w-full object-cover" />
                    </div>
                    <div className="flex-1 min-w-0">
                      <h3 className="font-bold text-white truncate text-sm">{ad.title}</h3>
                      <p className="text-orange-500 font-black text-sm">{ad.price} DA</p>
                    </div>
                  </div>
                  <div className="flex items-center justify-between text-sm mt-2 border-t border-white/5 pt-3">
                    <div className="flex items-center gap-2">
                      <span className={`px-2 py-0.5 rounded-md text-[10px] font-bold uppercase ${
                        isExpired(ad.dateAdded, !!ad.isPremium) 
                          ? 'bg-rose-500/20 text-rose-400 border border-rose-500/30'
                          : 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30'
                      }`}>
                        {isExpired(ad.dateAdded, !!ad.isPremium) ? (lang === 'ar' ? '�.�ŝ�S �"�"�S�' : 'ExpirǸ') : (lang === 'ar' ? '�ŝ�' : 'Actif')}
                      </span>
                      <span className="text-slate-400 text-xs">{ad.dateAdded}</span>
                    </div>
                    
                    <div className="flex gap-2">
                      {isExpired(ad.dateAdded, !!ad.isPremium) && (
                        <button 
                          onClick={() => onRenewAd(ad.id)}
                          className="p-2 bg-blue-500/10 text-blue-400 hover:bg-blue-500 hover:text-white rounded-xl transition shrink-0 flex items-center gap-1.5"
                          title="Renouveler"
                        >
                          <RefreshCw className="h-4 w-4" />
                          <span className="text-xs font-bold uppercase">{lang === 'ar' ? '�S�' : 'Renouveler'}</span>
                        </button>
                      )}
                      <button 
                        onClick={() => {
                          if(window.confirm('Voulez-vous vraiment supprimer cette annonce ?')) {
                            onDeleteAd(ad.id);
                          }

